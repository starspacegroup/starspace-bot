version: '1.0.0'

services:
  # bot:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile.bot
  #   depends_on:
  #     - mongodb

  mongodb:
    build:
      context: .
      dockerfile: Dockerfile.mongodb
    ports:
      - "27017:27017"
